import { Component, OnInit } from '@angular/core';

@Component({
  selector: '[app-selector-attribute]', // Selector is used as Attribute instead of element same as CSS
  templateUrl: './selector-attribute.component.html',
  styleUrls: ['./selector-attribute.component.css']
})
export class SelectorAttributeComponent implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}
